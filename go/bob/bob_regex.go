package bob

/*
import (
	"regexp"
	"strings"
)

const testVersion = 3

func Hey(s string) string {
	s = strings.Trim(s, " \t\n\r")
	if s == "" {
		return "Fine. Be that way!"
	}
	if matched, _ := regexp.MatchString("[A-Z]", s); matched == true && s == strings.ToUpper(s) { // shouting
		return "Whoa, chill out!"
	}
	if s[len(s)-1:] == "?" {
		return "Sure."
	}
	return "Whatever."
}
*/
