package acronym

/*
import (
	"regexp"
	"strings"
)

const testVersion = 3

// Abbreviate returns a three letter acronym.
func Abbreviate(original string) string {
	re := regexp.MustCompile("\\b\\w")
	matches := re.FindAllString(original, -1)
	return strings.ToUpper(strings.Join(matches, ""))
}
*/
